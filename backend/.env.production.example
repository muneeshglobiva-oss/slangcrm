# Production Environment Template - Backend
# Copy this to backend/.env and update values

# Node environment (do not change)
NODE_ENV=production

# Server configuration
PORT=3306

# Database Configuration
# Update these with your production database credentials
#DB_HOST=srv1816.hstgr.io
DB_HOST=srv1816.hstgr.io
DB_USER=u477896473_sl
DB_PASSWORD=Sl@@ng@1212
# DB_NAME=u477896473_slangprod

# Optional: Database connection pool settings
# DB_CONNECTION_LIMIT=20
# DB_QUEUE_LIMIT=0

# API Configuration (if using reverse proxy)
# API_URL=https://api.yourdomain.com

# CORS Configuration (set to your frontend domain)
# CORS_ORIGIN=https://yourdomain.com

# File upload configuration
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=5242880

# Logging
LOG_LEVEL=info

# Optional: Email configuration (for notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# ADMIN_EMAIL=admin@yourdomain.com
